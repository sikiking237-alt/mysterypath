import React, { useState, useEffect } from "react";
import { useNavigate, useLocation } from "react-router-dom";
// Relative paths updated to go up one level to find generic components
import Profile from "../Profile"; 
import NotificationBell from "../NotificationBell";

const StudentNavbar = ({ userName, onLogout, darkMode, onToggleDarkMode, onSidebarChange, sidebarCollapsed }) => {
  // ... Copy the full logic from your original Navbar.jsx here ...
};
export default StudentNavbar;